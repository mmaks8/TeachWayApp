SELECT CATEGORY.ID,CATEGORY.TITLE, COUNT(*) AS  “NUMBER”
FROM CATEGORY, REQUIREMENTS
WHERE CATEGORY.ID = REQUIREMENTS.CATEGORY
GROUP BY CATEGORY.TITLE
ORDER BY CATEGORY.ID ASC;

ALTER TABLE REQUIREMENTS
MODIFY DESCRIPTION VARCHAR(500);
